import { React } from 'react';

const Contrast = ({ init }) => {
  return (
    <div style={{ width: '100%', height: '100%', display: 'flex', alignItems: 'center', justifyContent: 'center', background: '#111', color: '#eee' }}>
      <p>Contrast testbed — nothing here yet.</p>
    </div>
  );
};

export default Contrast;
